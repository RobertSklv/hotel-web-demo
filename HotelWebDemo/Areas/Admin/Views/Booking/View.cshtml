@using HotelWebDemo.Models.ViewModels

@model BookingViewModel

@{
    Layout = "_AdminPageLayout";
    ViewData["PageHeading"] = $"Booking #{Model.Id}";
}

<partial name="_ViewBookingFieldset" model="@Model" />

<partial name="_Timeline" model="@Model" />